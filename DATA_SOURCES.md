# 📊 مصادر جلب العملات الرقمية

## 🎯 الهيكل الكامل لجلب البيانات

يتم جلب العملات من **3 مصادر** بترتيب محدد:

```
┌─────────────────────────────────────────┐
│  1️⃣ قاعدة البيانات المحلية (9 عملات)  │
│     - BTC, ETH, BNB, SOL, ADA,         │
│     - DOGE, XRP, LTC, MATIC            │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  2️⃣ Binance API (1000+ عملة)           │
│     - الأسعار الحية                    │
│     - حجم التداول 24 ساعة             │
│     - نسب التغير                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  3️⃣ إثراء البيانات                     │
│     - ربط البيانات من المصدرين         │
│     - إضافة المعايير الشرعية           │
│     - تطبيق الفلاتر                    │
└─────────────────────────────────────────┘
```

---

## 📍 المصدر الأول: قاعدة البيانات المحلية

### الملف:
```
src/lib/coins-database.ts
```

### البيانات المتوفرة:
```typescript
interface CoinData {
  symbol: string;              // الرمز (BTC, ETH...)
  name: string;                // الاسم
  category: string;            // التصنيف
  sharia_compliant: boolean;   // التوافق الشرعي
  sharia_notes: string;        // شرح الالتزام
  risk_level: string;          // مستوى المخاطرة
  performance_score: number;   // درجة الأداء (1-10)
  recommendation: string;      // التوصية
  liquidity: string;           // السيولة
  growth_potential: string;    // احتمالية النمو
  project_description: string; // وصف المشروع
  team: string;                // الفريق
  partners: string;            // الشركاء
  technology: string;          // التكنولوجيا
  links: {
    website?: string;
    whitepaper?: string;
    twitter?: string;
    docs?: string;
  };
}
```

### العملات المتوفرة:
```
✅ BTC   (Bitcoin)           - Layer 1 Payment
✅ ETH   (Ethereum)          - Layer 1 Smart Contract
✅ BNB   (Binance Coin)      - Exchange Token
✅ SOL   (Solana)            - Layer 1 Smart Contract
✅ ADA   (Cardano)           - Layer 1 Smart Contract
✅ DOGE  (Dogecoin)          - Payment Protocol
✅ XRP   (Ripple)            - Payment Protocol
✅ LTC   (Litecoin)          - Layer 1 Payment
✅ MATIC (Polygon)           - Layer 2 Solution
```

### المميزات:
- ⚡ **سريعة جداً** (< 1ms)
- 🔐 **موثوقة تماماً** (لا تعتمد على الإنترنت)
- ✅ **بيانات تامة** (شرعية، مخاطر، توصيات)
- 🌍 **ثنائية اللغة** (العربية + الإنجليزية)

---

## 📍 المصدر الثاني: Binance API

### النقاط النهائية (Endpoints):

#### 1️⃣ **معلومات الصرف** (Exchange Info)
```
GET https://api.binance.com/api/v3/exchangeInfo

الغرض: الحصول على معلومات جميع الأزواج المتاحة
البيانات:
- قائمة الرموز المتاحة
- معلومات التصفية
```

#### 2️⃣ **أسعار 24 ساعة** (24hr Ticker)
```
GET https://api.binance.com/api/v3/ticker/24hr

البيانات:
- السعر الحالي          (lastPrice)
- أعلى سعر 24h        (highPrice)
- أقل سعر 24h         (lowPrice)
- السعر الافتتاحي       (openPrice)
- نسبة التغير %        (priceChangePercent)
- حجم التداول         (volume)
- حجم بـ USDT         (quoteAssetVolume)
- سعر الطلب (Bid/Ask) (bidPrice, askPrice)
```

### معايير الفلترة من Binance:
```typescript
// الشروط التطبيقية:
1. symbol.endsWith('USDT')           // فقط أزواج USDT
2. price >= minPrice                 // الحد الأدنى للسعر
3. price <= maxPrice                 // الحد الأقصى للسعر
4. volume24h >= 100,000              // حجم تداول معقول
5. bidPrice > 0 && askPrice > 0      // أسعار صحيحة
6. spreadPercent < 1%                // فرق أسعار ضيق
7. !currentPortfolio.includes(coin)  // ليست موجودة عندك
```

### مثال على البيانات المستلمة:
```json
{
  "symbol": "BTCUSDT",
  "lastPrice": "42500.50",
  "priceChange": "500.00",
  "priceChangePercent": "1.19",
  "highPrice": "43000.00",
  "lowPrice": "41500.00",
  "volume": "15000.00",
  "quoteAssetVolume": "637500000.00",
  "bidPrice": "42499.50",
  "askPrice": "42500.50"
}
```

### المميزات:
- 🌍 **عملات كثيرة** (1000+ عملة)
- 💹 **بيانات حية** (تحديث مستمر)
- 📊 **بيانات سوقية** (حجم، تغير، سعر)
- 🔌 **API موثوق** (Binance الرسمي)

---

## 📍 المصدر الثالث: إثراء البيانات

### عملية الإثراء:

#### الخطوة 1️⃣: جلب من Binance
```typescript
const tickers = await fetch('https://api.binance.com/api/v3/ticker/24hr');
// النتيجة: 1000+ عملة بـ سعر، حجم، تغير
```

#### الخطوة 2️⃣: البحث في قاعدة البيانات المحلية
```typescript
const enrichedCoins = coins.map((coin) => {
  const dbCoin = getCoinData(coin.symbol); // البحث عن BTC مثلاً
  
  if (dbCoin) {
    // إذا وجدنا بيانات محلية، ادمجها
    return {
      ...coin,                              // بيانات Binance
      sharia_compliant: dbCoin.sharia_compliant,
      risk_level: dbCoin.risk_level,
      performance_score: dbCoin.performance_score,
      recommendation: dbCoin.recommendation,
      liquidity: dbCoin.liquidity,
      // ... وغيرها
    };
  }
  
  // إذا لم نجد بيانات محلية، استخدم قيماً افتراضية
  return {
    ...coin,
    sharia_compliant: "⚠️ غير محدد",
    risk_level: "🟡 متوسط",
    performance_score: 6,
    // ... وغيرها
  };
});
```

#### الخطوة 3️⃣: تطبيق الفلاتر
```typescript
// فلتر الشرعية
coins = coins.filter(coin => coin.shariaCompliance?.startsWith("✅"));

// فلتر المخاطرة
coins = coins.filter(coin => coin.riskLevel?.includes("منخفض"));

// فلتر الأداء
coins = coins.filter(coin => coin.performanceScore >= 5);

// فلتر السيولة
coins = coins.filter(coin => coin.liquidity?.includes("عالية"));

// ... والفلاتر الأخرى
```

#### الخطوة 4️⃣: التنويع والترتيب
```typescript
// اختر طريقة عشوائية من 5 خيارات:
1. عشوائي كامل
2. ترتيب حسب الأداء (الأفضل أولاً)
3. ترتيب حسب السعر (الأرخص أولاً)
4. ترتيب حسب التغير (أعلى ارتفاع)
5. عشوائي مختلط
```

---

## 🔄 التدفق الكامل للبيانات

```
المستخدم يضغط "احصل على الاقتراحات"
           │
           ▼
   ┌──────────────────┐
   │ جلب من Binance   │  API v3/ticker/24hr
   │   1000+ عملة    │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ تطبيق الشروط    │  السعر، الحجم، السبريد
   │ (نطاق السعر)    │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ إثراء من قاعدة  │  ربط البيانات المحلية
   │ البيانات المحلية│  (شرعية، مخاطر، إلخ)
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ تطبيق الفلاتر   │  شرعية، مخاطر، أداء
   │ المتقدمة        │  سيولة، توصيات
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ التنويع والترتيب│  5 طرق مختلفة
   │                 │  (عشوائي، أداء، سعر...)
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ عرض 5 عملات     │  مع جميع البيانات
   │ للمستخدم        │  (سعر، شرعية، توصيات)
   └──────────────────┘
```

---

## 📈 إحصائيات المصادر

| المصدر | العملات | السرعة | الموثوقية | البيانات |
|-------|--------|--------|----------|---------|
| **قاعدة محلية** | 9 | ⚡⚡⚡ سريع | 100% | متقدمة |
| **Binance API** | 1000+ | ⚡ متوسط | 99% | أساسية |
| **البيانات المدمجة** | 1000+ | ⚡⚡ جيد | 99% | شاملة |

---

## 🔍 مثال عملي

### البحث عن عملات آمنة وشرعية:

**الإدخالات**:
```
نطاق السعر: $0.10 - $10
التوافق الشرعي: ✅ متوافق فقط
مستوى المخاطرة: 🟢 منخفض
الحد الأدنى للأداء: 5+ نجوم
```

**العملية**:
```
1. جلب 1000+ عملة من Binance
   → 50 عملة بالنطاق المطلوب

2. إثراء من قاعدة البيانات
   → 9 عملات موجودة محلياً
   → 41 عملة بقيم افتراضية

3. تطبيق الفلاتر:
   ✅ شرعية فقط → 45 عملة
   🟢 منخفض المخاطر → 12 عملة
   ⭐ 5+ نجوم → 8 عملات

4. التنويع والاختيار
   → 5 عملات عشوائية

5. النتيجة النهائية:
   BTC ✅ منخفض 9/10
   ETH ✅ منخفض 9/10
   BNB ✅ منخفض 8/10
   SOL ✅ متوسط 7/10
   ADA ✅ متوسط 6/10
```

---

## ✅ الخلاصة

| المرحلة | المصدر | البيانات | الحالة |
|--------|--------|---------|--------|
| 1. الجلب | Binance API | 1000+ عملة | ✅ |
| 2. الإثراء | قاعدة محلية | معايير متقدمة | ✅ |
| 3. الفلترة | محرك البحث | تطبيق الشروط | ✅ |
| 4. التنويع | الخوارزمية | 5 طرق مختلفة | ✅ |
| 5. العرض | الواجهة | نتائج نهائية | ✅ |

---

**التحديث**: ديسمبر 3، 2024
