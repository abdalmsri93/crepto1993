# 📋 دليل العمل على النظام بأمان
# Git Workflow Guide

## 🎯 الهيكل الحالي / Current Structure

```
main (النظام المستقر)       ← النظام الحي والعامل
  └─ development (التطوير)   ← للتعديلات والاختبار
```

---

## 🚀 طريقة العمل اليومية / Daily Workflow

### 1️⃣ بداية العمل - ابدأ في فرع التطوير

**شغل الملف:**
```
switch-to-dev.bat
```

**أو بالأوامر:**
```powershell
git checkout development
```

---

### 2️⃣ اعمل على التعديلات

- عدل على الكود كما تريد
- اختبر التعديلات جيداً
- **لا تقلق!** التعديلات لن تؤثر على النظام الأساسي

---

### 3️⃣ حفظ التعديلات

```powershell
git add .
git commit -m "وصف التعديلات"
git push origin development
```

---

### 4️⃣ عند نجاح التعديلات - انقلها للنظام الأساسي

**شغل الملف:**
```
merge-dev-to-main.bat
```

**أو بالأوامر:**
```powershell
git checkout main
git merge development
git push origin main
```

---

## 🔄 سيناريوهات شائعة / Common Scenarios

### ✅ إضافة ميزة جديدة

```powershell
# 1. اذهب للتطوير
git checkout development

# 2. اعمل على الميزة
# ... كود التعديلات ...

# 3. احفظ
git add .
git commit -m "إضافة ميزة X"

# 4. اختبر جيداً!

# 5. انقل للـ main
git checkout main
git merge development
git push
```

### 🐛 إصلاح خطأ

```powershell
# 1. افتح فرع جديد من development
git checkout development
git checkout -b fix/bug-name

# 2. أصلح الخطأ

# 3. احفظ
git add .
git commit -m "إصلاح خطأ X"

# 4. ارجع للـ development
git checkout development
git merge fix/bug-name

# 5. اختبر ثم انقل للـ main
git checkout main
git merge development
```

### ⚠️ التراجع عن تعديلات فاشلة

```powershell
# إذا كان التعديل لم يُحفظ بعد
git checkout .

# إذا كان محفوظاً (commit)
git reset --hard HEAD~1

# إذا كان مرفوعاً (pushed)
git revert HEAD
```

---

## 📁 ملفات مساعدة / Helper Files

| الملف | الوظيفة |
|------|---------|
| `switch-to-dev.bat` | التبديل لفرع التطوير |
| `switch-to-main.bat` | التبديل للفرع الأساسي |
| `merge-dev-to-main.bat` | دمج التعديلات الناجحة |

---

## ⚡ نصائح سريعة / Quick Tips

1. **دائماً اعمل في `development`** - لا تعدل مباشرة في `main`
2. **اختبر قبل الدمج** - تأكد أن كل شيء يعمل
3. **احفظ باستمرار** - `git commit` كل فترة
4. **اكتب وصف واضح** - للـ commit message

---

## 🆘 في حالة الطوارئ

**إذا حدث خطأ في main:**

```powershell
# ارجع لآخر نسخة سليمة
git checkout main
git log  # اختر رقم الـ commit السليم
git reset --hard <commit-id>
git push --force origin main  # استخدم بحذر!
```

---

## ✨ الفائدة

✅ النظام الأساسي محمي دائماً  
✅ يمكنك التجربة بحرية  
✅ سهولة التراجع عن الأخطاء  
✅ تاريخ واضح للتعديلات  
✅ إمكانية العمل على أكثر من ميزة  

---

**🎉 الآن يمكنك العمل بأمان وثقة!**
