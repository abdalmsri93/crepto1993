# 🚀 **تفعيل البدء التلقائي للخدمة**

## ⚠️ **متطلب مهم:**
لتفعيل البدء التلقائي، **يجب تشغيل الملف كـ Administrator**

---

## ✅ **الطريقة السهلة (3 خطوات):**

### **الخطوة 1: افتح File Explorer**
```
Windows + E
```

### **الخطوة 2: اذهب إلى:**
```
C:\Users\YOUR_USERNAME\Desktop\binance-watch-live
```

### **الخطوة 3: انقر بزر الماوس الأيمن على:**
```
enable-auto-startup.bat
```

ثم اختر:
```
Run as Administrator
```

---

## 📋 **ماذا سيحدث:**

```
✅ تم الوصول للصلاحيات المطلوبة
🔧 جاري تسجيل المهمة المجدولة...

✅ تم تفعيل البدء التلقائي بنجاح!

📋 تفاصيل المهمة:
   اسم المهمة: BinanceAutoSearch
   الموقع: C:\Users\...\Desktop\binance-watch-live
   التشغيل: عند بدء الجهاز تلقائياً
   الحالة: ✅ مفعّل

💡 الخدمة ستبدأ تلقائياً عند بدء الجهاز القادم
```

---

## 🧪 **التحقق من التفعيل:**

بعد التشغيل، افتح PowerShell وشغل:

```powershell
Get-ScheduledTask -TaskName "BinanceAutoSearch" | Select-Object TaskName, State
```

يجب أن ترى:
```
TaskName              State
--------              -----
BinanceAutoSearch     Ready
```

---

## 🔄 **للاختبار الفوري:**

إذا أردت اختبار الخدمة الآن بدون انتظار إعادة التشغيل:

```powershell
# تشغيل المهمة الآن
schtasks /run /tn "BinanceAutoSearch"
```

أو اختبر يدوياً:
```powershell
cd C:\Users\YOUR_USERNAME\Desktop\binance-watch-live
node auto-search-service.js
```

---

## ❌ **إذا أردت إيقاف البدء التلقائي:**

```powershell
# حذف المهمة
schtasks /delete /tn "BinanceAutoSearch" /f
```

---

## 🎯 **الخطوات الموصى بها:**

1. ✅ **الآن:** انقر بزر الماوس الأيمن على `enable-auto-startup.bat` → Run as Administrator
2. ✅ **ثم:** أضف مفاتيح API إلى `.env`
3. ✅ **أخيراً:** أعد تشغيل الجهاز واختبر

---

## 💡 **ملاحظات مهمة:**

- الخدمة ستبدأ **تلقائياً** عند بدء الجهاز
- ستعمل **في الخلفية** بدون نافذة مرئية
- ستبحث عن **5 عملات** كل **60 ثانية**
- **لا تحتاج** لفتح المتصفح

---

**جاهز؟ انقر على الملف الآن! ⬇️**

```
C:\Users\YOUR_USERNAME\Desktop\binance-watch-live\enable-auto-startup.bat
```

👆 **اضغط بزر الماوس الأيمن + Run as Administrator**
