# دليل نشر تطبيق محفظة العملات الرقمية

## 📋 المتطلبات الأساسية

قبل البدء في عملية النشر، تأكد من توفر:

1. ✅ حساب على منصة نشر (Vercel أو Netlify)
2. ✅ حساب Supabase فعّال (يحتوي على المشروع المستخدم)
3. ✅ مفتاح OpenAI API (مع تفعيل الفوترة)
4. ✅ مفاتيح Binance API (للمستخدمين)

---

## 🚀 خطوات النشر على Vercel

### 1. رفع المشروع إلى GitHub
```bash
git add .
git commit -m "Deploy to production"
git push origin main
```

### 2. ربط المشروع مع Vercel
1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط على "New Project"
3. اختر المستودع (Repository) من GitHub
4. اضغط على "Import"

### 3. إعداد متغيرات البيئة (Environment Variables)
في صفحة إعدادات المشروع في Vercel، أضف المتغيرات التالية:

```env
VITE_SUPABASE_URL=https://ftgvxvwvbtfkbgkuccwx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ0Z3Z4dnd2YnRma2Jna3VjY3d4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQwNzEwMjYsImV4cCI6MjA3OTY0NzAyNn0.38ttXMQK9f9Mzi054T-srCIb6uojWTh1ZPDktApdDdY
VITE_SUPABASE_PROJECT_ID=ftgvxvwvbtfkbgkuccwx
```

### 4. إعداد Supabase Secrets
في لوحة تحكم Supabase الخاصة بمشروعك:

1. اذهب إلى **Settings** → **Edge Functions** → **Secrets**
2. أضف السر التالي:
   - **Name:** `OPENAI_API_KEY`
   - **Value:** مفتاح OpenAI API الخاص بك

### 5. نشر Edge Functions
Edge Functions موجودة في مجلد `supabase/functions/` وتعمل من خلال Supabase مباشرة.

**مهم:** تأكد من أن Edge Functions تم نشرها في Supabase:
- تحقق من أن الدوال موجودة في: **Project → Edge Functions**
- إذا لم تكن موجودة، قم بنشرها من Lovable أو عبر Supabase CLI

### 6. النشر
1. اضغط على "Deploy"
2. انتظر حتى تكتمل عملية البناء
3. افتح رابط الموقع المنشور للتأكد

---

## 🌐 خطوات النشر على Netlify

### 1. ربط المشروع مع Netlify
1. اذهب إلى [netlify.com](https://netlify.com)
2. اضغط على "Add new site" → "Import an existing project"
3. اختر GitHub واختر المستودع
4. اضغط على "Deploy site"

### 2. إعداد متغيرات البيئة
في **Site settings** → **Environment variables**، أضف:

```env
VITE_SUPABASE_URL=https://ftgvxvwvbtfkbgkuccwx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ0Z3Z4dnd2YnRma2Jna3VjY3d4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQwNzEwMjYsImV4cCI6MjA3OTY0NzAyNn0.38ttXMQK9f9Mzi054T-srCIb6uojWTh1ZPDktApdDdY
VITE_SUPABASE_PROJECT_ID=ftgvxvwvbtfkbgkuccwx
```

### 3. إعادة النشر
بعد إضافة المتغيرات:
1. اذهب إلى **Deploys**
2. اضغط على "Trigger deploy" → "Deploy site"

---

## ⚙️ التحقق من عمل التطبيق

### اختبار الواجهة الأمامية
- ✅ يفتح الموقع بدون أخطاء
- ✅ صفحة تسجيل الدخول تظهر بشكل صحيح
- ✅ التصميم والألوان تظهر بشكل صحيح

### اختبار الاتصال بـ Backend
1. سجّل دخول بحساب موجود
2. اذهب إلى **الإعدادات**
3. أدخل مفاتيح Binance API
4. اضغط على "اختبار الاتصال"
5. يجب أن ترى "نجح الاتصال"

### اختبار التحليل الذكي
1. بعد إضافة مفاتيح Binance
2. اذهب إلى الصفحة الرئيسية
3. يجب أن يظهر التحليل الذكي تلقائياً
4. إذا ظهرت رسالة "لا يمكن تنفيذ التحليل"، تأكد من:
   - إضافة `OPENAI_API_KEY` في Supabase Secrets
   - تفعيل الفوترة في حساب OpenAI

---

## 🐛 حل المشاكل الشائعة

### المشكلة: صفحة بيضاء أو خطأ 404
**الحل:**
- تأكد من إضافة جميع متغيرات البيئة في منصة النشر
- أعد نشر الموقع (Redeploy)

### المشكلة: "Failed to fetch portfolio"
**الحل:**
- تحقق من أن Edge Functions موجودة في Supabase
- تأكد من صحة `VITE_SUPABASE_URL` و `VITE_SUPABASE_PUBLISHABLE_KEY`

### المشكلة: "نفد رصيد التحليل الذكي"
**الحل:**
- تأكد من إضافة `OPENAI_API_KEY` في Supabase Secrets
- تأكد من تفعيل الفوترة في حساب OpenAI
- تحقق من وجود رصيد كافٍ في حساب OpenAI

### المشكلة: "مفاتيح Binance غير صحيحة"
**الحل:**
- تأكد من أن المفاتيح منسوخة بشكل صحيح (بدون مسافات)
- تأكد من أن API Key له صلاحية "Read" على الأقل
- جرّب إنشاء مفتاح جديد من Binance

---

## 📝 ملاحظات مهمة

1. **Edge Functions لا تُنشر مع الكود**
   - Edge Functions موجودة في Supabase وتعمل بشكل منفصل
   - لا تحتاج لنشرها مرة أخرى إلا إذا قمت بتعديلها

2. **البيانات الحساسة**
   - لا تضع مفاتيح API في الكود أبداً
   - استخدم دائماً متغيرات البيئة
   - ملف `.env` لن يُرفع إلى GitHub (موجود في `.gitignore`)

3. **التحديثات المستقبلية**
   - عند تعديل الكود في GitHub، سيتم نشر التحديث تلقائياً
   - عند تعديل Edge Functions، يجب نشرها من Lovable أو Supabase CLI

---

## 🆘 الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تحقق من **Console** في المتصفح (F12)
2. تحقق من **Edge Function Logs** في Supabase
3. راجع هذا الدليل مرة أخرى
4. تواصل مع الدعم الفني

---

## ✅ قائمة التحقق النهائية

قبل إطلاق الموقع للعامة، تأكد من:

- [ ] إضافة جميع متغيرات البيئة في منصة النشر
- [ ] إضافة `OPENAI_API_KEY` في Supabase Secrets
- [ ] اختبار تسجيل الدخول والتسجيل
- [ ] اختبار الاتصال بـ Binance
- [ ] اختبار التحليل الذكي
- [ ] اختبار صفحة اكتشاف العملات الجديدة
- [ ] اختبار على أجهزة مختلفة (Desktop, Mobile)

---

**تم بنجاح! 🎉**

موقعك الآن منشور ويعمل بشكل كامل.
