# 🔍 شروط البحث والفلترة عن العملات الجديدة

## 📋 نظرة عامة

التطبيق يستخدم عدة طبقات من البحث والفلترة للعملات:

### 1️⃣ البحث المحلي (Local Search) - من قاعدة البيانات

**الملف**: `src/lib/coins-database.ts`

**الدالة**: `searchCoins(query: string)`

**الشروط**:
```typescript
// البحث يتم على:
coin.symbol.toLowerCase().includes(query.toLowerCase())  // الرمز (مثل BTC، ETH)
||
coin.name.toLowerCase().includes(query.toLowerCase())    // الاسم (مثل Bitcoin، Ethereum)
```

**مثال**:
```
البحث عن: "BIT"
النتائج: 
- BTC (Bitcoin)
- DOGE (لا يطابق)
```

---

### 2️⃣ البحث من Binance API - العملات المقترحة

**الملف**: `src/pages/SuggestCoins.tsx`

**المصدر**: Binance REST API

**الشروط الأساسية**:

#### أ) متطلبات الرمز (Symbol Requirements):
```typescript
// 1. يجب أن ينتهي الرمز بـ USDT
symbol.endsWith('USDT')

// 2. يتم إزالة USDT من الرمز
symbol.replace('USDT', '')  // BTCUSDT → BTC

// 3. لا يجب أن تكون موجودة في المحفظة الحالية
!currentSymbols.has(coin.symbol.toUpperCase())
```

#### ب) متطلبات السعر (Price Range):
```typescript
const price = parseFloat(ticker.lastPrice)
price >= minPrice && price <= maxPrice

// القيم الافتراضية:
min = 0.0001
max = 1000
```

#### ج) متطلبات الحجم (Volume Requirements):
```typescript
volume24h >= 100000  // حجم التداول في 24 ساعة
quoteAssetVolume >= 100000  // حجم الأصل الأساسي
```

#### د) متطلبات السيولة (Liquidity):
```typescript
spreadPercent < 1.0  // فروق الأسعار أقل من 1%
bidPrice > 0 && askPrice > 0  // وجود عرض وطلب

// حساب فروق الأسعار:
spreadPercent = ((askPrice - bidPrice) / bidPrice * 100)
```

---

### 3️⃣ البحث من CoinGecko API - عملات خارج Binance

**الملف**: `src/pages/ProjectDetails.tsx`

**الدالة**: `fetchFromCoinGecko(coinSymbol: string)`

**الشروط**:

#### أ) متطلبات البحث:
```typescript
// 1. البحث عن العملة بالرمز
searchUrl = `https://api.coingecko.com/api/v3/search?query=${coinSymbol}`

// 2. يجب أن تكون النتيجة موجودة
if (!searchData.coins?.length) {
  throw new Error(`لم يتم العثور على العملة: ${coinSymbol}`)
}

// 3. استخدام أول نتيجة
const coinId = searchData.coins[0].id
```

#### ب) متطلبات البيانات:
```typescript
// 1. اسم العملة
details.name

// 2. الوصف (أولاً عربي، ثم إنجليزي)
details.description?.ar || details.description?.en

// 3. رتبة القيمة السوقية
marketCapRank = details.market_data?.market_cap_rank

// 4. مستوى المخاطرة (بناءً على الرتبة)
if (marketCapRank <= 100) risk = "منخفض"
else if (marketCapRank <= 500) risk = "متوسط"
else risk = "عالي"

// 5. درجة الأداء
performance = Math.min(10, Math.max(1, 10 - (marketCapRank / 100)))

// 6. التصنيفات
details.categories (Technology, DeFi, etc.)

// 7. القيمة السوقية
details.market_data?.market_cap?.usd
```

#### ج) المهلات الزمنية (Timeouts):
```typescript
// 3 ثوانٍ كحد أقصى للاتصال
new Promise((_, reject) => 
  setTimeout(() => reject(new Error("Timeout")), 3000)
)
```

---

### 4️⃣ الترجمة التلقائية للعملات الخارجية

**الملف**: `src/lib/translations.ts`

**الدالة**: `translateLongText(text: string, language: Language)`

**شروط الترجمة**:

#### النصوص المترجمة:
```typescript
// تعبيرات طويلة (من الأطول للأقصر)
"leveraged yield farming" → "الزراعة العائدة برافعة مالية"
"yield farming protocol" → "بروتوكول الزراعة العائدة"
"deposit vaults" → "خزائن الإيداع"

// كلمات متوسطة
"protocol" → "بروتوكول"
"decentralized" → "لامركزي"
"blockchain" → "سلسلة الكتل"

// كلمات قصيرة
"token" → "رمز"
"deposit" → "إيداع"
"stake" → "رهن"

// المجموع: 80+ كلمة مترجمة
```

#### شروط الترجمة:
```typescript
1. لا ترجمة إذا كانت اللغة = English
2. حدود الكلمات (\b...\b) لتجنب ترجمة جزئية
3. عدم التفريق بين الأحرف الكبيرة والصغيرة (gi)
4. الترجمة من الأطول للأقصر
```

---

## 🎯 مثال عملي للبحث

### البحث عن عملة جديدة:

```
الخطوة 1: المستخدم يدخل "ALPACA" في صفحة الاقتراحات
         ↓
الخطوة 2: البحث في قاعدة البيانات المحلية
         - هل "ALPACA" في نظامنا؟
         - لا → انتقل للخطوة 3
         ↓
الخطوة 3: البحث في Binance API
         - ابحث عن ALPACAUSDT
         - تحقق من الشروط:
           * السعر في النطاق المسموح ✓
           * حجم التداول كافٍ ✓
           * السيولة جيدة ✓
         - النتيجة: عملة مقترحة
         ↓
الخطوة 4: عند الضغط على "معلومات المشروع"
         - ابحث عن ALPACA في قاعدة البيانات
         - لا → جلب من CoinGecko API
         - النص بالإنجليزية ← ترجمة تلقائية للعربية
         - عرض النتائج مع تبديل اللغات
```

---

## 📊 معايير الجودة للعملات المقترحة

| المعيار | القيمة | الوصف |
|--------|--------|--------|
| الحد الأدنى للسعر | 0.0001 | لا عملات براقة جداً |
| الحد الأقصى للسعر | 1000 | لا عملات غالية جداً |
| حجم التداول | 100,000 | يجب أن يكون سيولة كافية |
| فروق الأسعار | < 1% | يجب أن تكون سهلة التداول |
| الرتبة (من CoinGecko) | top 5000 | العملات الموثوقة |

---

## 🔧 تخصيص الشروط

### لتغيير حدود السعر:
```typescript
// في SuggestCoins.tsx، ابحث عن:
const min = 0.0001;
const max = 1000;

// غيّر إلى ما تريد، مثلاً:
const min = 0.01;
const max = 100;
```

### لتغيير معايير السيولة:
```typescript
// ابحث عن:
volume24h >= 100000

// غيّر إلى:
volume24h >= 500000  // سيولة أعلى
```

### لإضافة كلمات جديدة للترجمة:
```typescript
// في translations.ts، أضف إلى commonTranslations:
'new word': 'كلمة جديدة',
```

---

## 📈 التطور المستقبلي

### شروط بحث محسّنة:
- ✅ الذكاء الاصطناعي لتصنيف العملات
- ✅ مؤشرات تقنية للبحث الأمثل
- ✅ تصفية بناءً على المؤسسين والشركاء
- ✅ تصفية بناءً على التكنولوجيا المستخدمة

### تحسينات الترجمة:
- ✅ ترجمة فورية من Google Translate
- ✅ قاموس شامل أكثر (200+ كلمة)
- ✅ تعريب الأرقام والعملات

---

## ❓ الأسئلة الشائعة

**س: كيف أضيف عملة جديدة؟**
```
1. أضفها في coins-database.ts
2. استخدم نفس هيكل البيانات
3. أضف الترجمات (عربي + إنجليزي)
```

**س: كيف أغيّر معايير البحث؟**
```
1. ابحث عن الشروط في SuggestCoins.tsx
2. غيّر الأرقام والشروط
3. أعد بناء المشروع
```

**س: لماذا بعض العملات لا تظهر؟**
```
1. قد تكون خارج نطاق السعر
2. قد لا يكون لديها سيولة كافية
3. قد لا تكون على Binance
→ ابحث عنها من خلال CoinGecko
```

---

**آخر تحديث**: ديسمبر 2024
**الإصدار**: 1.0.0
